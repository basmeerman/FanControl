version: 2
updates:
  # v0.2.0 migrated to ESPHome; no Python / pip manifests in the repo.
  # ESPHome version is pinned inside the esphome/build-action@v7 action call.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "ci-cd"
